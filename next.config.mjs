// /** @type {import('next').NextConfig} */
// const nextConfig = {};

// export default nextConfig;
/** @type {import('next').NextConfig} */
const nextConfig = {
  // output: 'standalone',
  // experimental: {
  //   appDir: true, 
  // },
    images: {
      
      domains: ['/'], // Add the external image domain here
    },
  };
  
  export default nextConfig;
  